# AGENTS.md

## Cursor Cloud specific instructions

This repository (`jayseik/molly`) is currently an empty/bare repository with only a `README.md` file. There are no applications, services, dependencies, tests, or build systems configured yet.

### Current state

- **No source code** — no programming languages or frameworks in use.
- **No package manager** — no `package.json`, `requirements.txt`, or equivalent.
- **No build tools** — no `Makefile`, bundler configs, etc.
- **No services** — nothing to start or run.
- **No tests or linting** — no test framework or linter configured.

### For future agents

Once the repository has code added, update this file with:
- How to install dependencies (and the update script via `SetupVmEnvironment`).
- How to run the dev server, tests, and linter.
- Any non-obvious environment setup steps or gotchas.
